# 在庫データ・表示内容の見直し（2026-09-23）

対象: 稼働中の「在庫管理bot」Google Sheets と Web / LIFF。調査は読み取りのみで実施。Inventory と ConsumptionLog は変更していない。

## 現状

| 項目 | 確認結果 | 次の対応 |
| --- | --- | --- |
| Inventory | 73品目。分類と最低在庫は全品目に設定済み | 家庭で使う単位と最低在庫を品目ごとに確認 |
| ConsumptionLog | 335件。完全一致の重複は16組 | 同じ日に複数回消費した可能性があるため、元記録を確認してから訂正 |
| 商品設定 | 見出しのみで、入数は未登録 | 商品を購入するときの入数を登録。未登録時は1として計算 |
| 商品分析 | 今すぐ購入14品目、次回購入10品目、問題なし49品目 | 買い物候補の妥当性を確認 |
| 異常候補 | 重複と大量消費が表示される。重複は元ログの行ごとに表示 | 確認しやすいように同一事象の表示を整理 |

## 優先して確認する内容

1. **入数**: トイレットペーパーは現在「本」単位で、購入推奨数は16本。購入商品が12ロール入りなら、商品設定に入数12を登録すると推奨数の切り上げに反映できる。ティッシュ、飲料なども実際の購入単位を確認する。
2. **単位**: 「LUX コンディショナー」は「回分」、「トイレマジックリン」は「個」、「抗菌目薬」は「本」と登録されている。意図した数え方か確認する。単位だけを変更すると過去の消費数量との解釈がずれるため、在庫数・最低在庫・履歴との整合も確認する。
3. **消費履歴がない品目**: 14品目。消費ペースに基づく予測ができないため、最低在庫を使う。必要なら今後の使用時に記録する。
4. **異常候補**: 同じ品目・日付・数量の記録は、実際に複数回使用した場合もある。自動削除はしない。特に大量消費のトイレットペーパーなどは、単位と記録方法を先に確認する。

## 今回の画面修正

- 標準同期の初回表示にサンプル品目を混ぜない。サンプルは端末内のお試しモードに限定。
- 保存済みデータがない状態では、在庫や買い物候補が「0件」と確定したように見せず、読み込み案内を表示。
- 同期状態を利用者向けの文言に変更。

## 残る作業

- 入数、単位、最低在庫は利用実態が分かってからシートに反映する。
- 異常候補の確認結果を記録できる仕組みを設計し、確認済みの事象を区別する。
- UI と LINE で「今すぐ購入」「次回購入」「問題なし」を同じ意味で使うよう、表示を揃える。
